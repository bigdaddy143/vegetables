export class Reward {
  rewardId: number;
  name: string;
  price: number;
  details: string;
  imageLink: string;
  selected: boolean; // need to determine if the backend is storing selected state
  percentageHeight: number;
}


